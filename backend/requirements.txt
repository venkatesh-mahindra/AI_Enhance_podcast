# PDF to Podcast Converter - Python Requirements

# Web Framework
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# AWS SDK
boto3==1.34.0

# PDF Processing
PyMuPDF==1.23.8
pillow==10.1.0

# Computer Vision (CPU-only)
opencv-python==4.8.1.78

# Audio Processing (CPU-only)
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
pyttsx3==2.90

# NLP (spaCy core; model installed separately)
spacy==3.7.2

# Task Queue
celery==5.3.4
redis==5.0.1

# Monitoring
prometheus-flask-exporter==0.22.4

# Utilities
# Pin numpy to a wheel that supports Python 3.12 on Windows
numpy==1.26.4
pandas==2.1.4
python-dotenv==1.0.0
werkzeug==3.0.1

# Google TTS for Indian voices
gTTS==2.5.4

# Optional Voice Cloning (install separately for TRUE voice cloning):
# TTS==0.22.0  # Coqui TTS for voice cloning
# torch>=2.0.0  # Required for TTS
# phonemizer  # Required for TTS

# Optional heavy ML (omit on Windows/local):
# torchvision
# torchaudio
# transformers
# sentence-transformers
# langchain